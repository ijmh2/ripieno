{
  "name": "multiplayer-agent-workspace",
  "private": true,
  "version": "0.0.0",
  "description": "A shared AI agent room \u2014 one conversation, multiple people, one agent that knows who said what.",
  "workspaces": [
    "packages/protocol",
    "packages/mcp",
    "packages/relay",
    "packages/extension"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "build:relay": "npm run build -w @mpa/protocol && npm run build -w @mpa/relay",
    "start": "node packages/relay/dist/src/index.js"
  },
  "engines": {
    "node": ">=20"
  }
}
